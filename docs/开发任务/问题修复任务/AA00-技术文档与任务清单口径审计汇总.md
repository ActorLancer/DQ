# 技术文档与任务清单口径审计汇总

## 1. 审计范围

- 已审计任务清单与执行源：
  - `docs/开发任务/v1-core-开发任务清单.csv`
  - `docs/开发任务/v1-core-开发任务清单.md`
  - `docs/开发任务/V1-Core-TODO与预留清单.md`
  - `docs/开发任务/问题修复任务/A01~A15*.md`
  - `docs/开发任务/Agent-开发与半人工审核流程.md`
  - `docs/开发任务/AI-Agent-执行提示词.md`
  - `docs/开发任务/V1-Core-实施进度日志-P1.md`
  - `docs/开发任务/V1-Core-实施进度日志-P2.md`
  - `docs/开发任务/V1-Core-实施进度日志-P3.md`
- 已审计技术设计、冻结文档、契约与迁移设计：
  - `docs/00-context/**`
  - `docs/02-openapi/**`
  - `docs/04-runbooks/**`
  - `docs/05-test-cases/**`
  - `docs/开发准备/**`
  - `docs/原始PRD/**`
  - `docs/数据库设计/接口协议/**`
  - `docs/数据库设计/V1/**/*.sql`
- 已审计运行约束与脚本：
  - `infra/**/*.yml`
  - `infra/**/*.json`
  - `infra/**/*.sh`
  - `scripts/**/*.sh`
  - `scripts/**/*.py` 中与校验、部署、初始化相关的脚本
  - `packages/openapi/**`
  - `services/README.md`
  - `workers/README.md`
- 明确排除：
  - `apps/**`、`services/**`、`workers/**`、`packages/**` 中的业务实现代码
  - 不以“功能是否已经开发完成”为审计对象；仅在文档、任务、脚本、契约、配置、迁移设计之间找会误导后续执行的口径问题

## 2. 审计方法

- 本次只审计文档、脚本、配置、契约、README、runbook、OpenAPI、test-cases、SQL 迁移设计，不审计业务功能实现完整性。
- `docs/开发任务/v1-core-开发任务清单.csv` 视为唯一执行源；`v1-core-开发任务清单.md` 视为阅读版；`V1-Core-TODO与预留清单.md` 视为后续实现义务登记；`问题修复任务` 视为口径收缩补丁。
- 先做“问题修复文档 -> CSV 任务 -> Markdown 阅读版 -> TODO -> runbook/OpenAPI/test-cases/scripts”的映射核对，再做命名、权威源、阶段边界与推荐阅读校对。
- 只有在“会误导后续开发执行”“会让执行源不可用”“会让后续 Agent 读错权威源”“会让阶段边界或环境口径漂移”时才计为问题。
- 每条问题均要求证据链；证据不足但存在误导风险的，列为 `suspect`。

## 3. 已阅读关键证据

- `docs/开发任务/v1-core-开发任务清单.csv:9,24,115,350-351,403`
  - `CTX-008` 已冻结 `local / staging / demo` 三套运行模式。
  - `CTX-023`、`NOTIF-013`、`NOTIF-014`、`SEARCHREC-021` 的 `technical_reference` 中存在失效路径。
  - `ENV-058` 把命名统一定义为执行任务，但未给出覆盖全部漂移点的推荐阅读入口。
- `docs/开发任务/V1-Core-TODO与预留清单.md:43,56-67`
  - `TODO-PROC-BIL-001` 仍把 `docs/开发任务/V1-Core-实施进度日志.md` 当成追溯依据。
  - `TODO-AUD-OPENAPI-001`、`TODO-AUD-TEST-001`、`TODO-SEARCHREC-CONTRACT-TEST-001`、`TODO-NOTIF-CONTRACT-001` 已明确登记未来补齐义务。
  - `TODO-SEARCHREC-CONSUMER-001`、`TODO-AUD-EVENT-001`、`TODO-TEST-CANONICAL-001` 说明 A15/A02/A01 的后续实现义务已进入执行源。
- `docs/开发任务/Agent-开发与半人工审核流程.md:156-166,337-343`
  - 流程文档强制要求每批次把“计划中 / 待审批”写入 `开发任务/V1-Core-实施进度日志.md`。
  - 换手规则也要求新 Agent 先读 `开发任务/V1-Core-实施进度日志.md`。
- `docs/开发任务/AI-Agent-执行提示词.md:17-35,42,65`
  - 提示词继续把 `开发任务/V1-Core-实施进度日志.md` 当成必读、必写、必同步的执行源。
- `docs/开发任务/V1-Core-实施进度日志-P2.md:705`
  - 当前仓库内已有自证：旧路径不存在，实际改为在 `V1-Core-实施进度日志-P2.md` 记录。
- `docs/开发准备/配置项与密钥管理清单.md:180-184,202-203,230-231`
  - 冻结文档仍使用 `PG_*`、`MINIO_ACCESS_KEY / MINIO_SECRET_KEY`、`KEYCLOAK_ADMIN_USERNAME / KEYCLOAK_ADMIN_PASSWORD`。
- `docs/开发准备/本地开发环境与中间件部署清单.md:138,189-193,211-212,239-240,267-274,319,323`
  - 本地部署清单的环境变量口径与实际 compose/env 文件不一致。
  - 文档内仍引用 `../部署脚本/docker-compose.postgres-test.yml` 与 `../部署脚本/validate_database_migrations.sh` 这类当前仓库下不存在的路径。
  - 同一文档又新增 `staging-local` 小节，和冻结的三套运行模式发生漂移。
- `infra/docker/.env.local:49-55`、`infra/docker/docker-compose.local.yml:29-31,82-89,133-134,198-202`
  - 实际本地 env/compose 使用 `POSTGRES_*`、`MINIO_ROOT_*`、`KEYCLOAK_ADMIN`，并把宿主机 Kafka 外部端口冻结为 `9094`。
- `scripts/smoke-local.sh:46-48,65-66`、`scripts/check-query-compile.sh:9,15`、`scripts/seed-local-iam-test-identities.sh:6-8`
  - 运行脚本使用 `POSTGRES_*`、`MINIO_ROOT_*` 与 `DATABASE_URL`，但配置冻结文档未把这些关系收敛为单一权威口径。
- `docs/00-context/run-modes.md:5-21`、`docs/00-context/local-deployment-boundary.md:5-10`
  - 冻结文档只承认 `local / staging / demo` 三套模式；基础设施 compose 仅承接 `local` 和通过 profile 控制的 demo/mock/fabric 变体。
- `docs/04-runbooks/local-startup.md:19,37,46,56,63`、`docs/04-runbooks/search-reindex.md:54`、`docs/04-runbooks/port-matrix.md:15`
  - 当前 runbook 已把宿主机 Kafka 地址固定为 `127.0.0.1:9094`。
  - `local-startup.md` 给出 `make up-demo` 和 `check-mock-payment.sh`，但没有明确说明 mock payment 检查与 profile 的强绑定关系。
- `docs/05-test-cases/catalog-review-cases.md:23-31`、`docs/05-test-cases/README.md:19-20`
  - `catalog-review-cases.md` 仍用宿主机 `127.0.0.1:9092` 启动示例。
  - `notification-cases.md`、`audit-consistency-cases.md` 尚未落盘，但 README 已把“未来必须补齐”说明清楚。
- `docs/02-openapi/README.md:5-24`、`docs/02-openapi/search.yaml:1-3`
  - 当前阶段 `packages/openapi/*.yaml` 才是实现期权威源。
  - `docs/02-openapi/search.yaml` 明确声明自己不是设计参考，只是未来归档占位。
- `docs/开发任务/v1-core-开发任务清单.csv:67-68,338-346,352,359-361,377,383,392,397,399,402,453`
  - `A01`、`A02`、`A10`、`A15` 已被多个 CSV 任务承接，覆盖 ENV / NOTIF / AUD / SEARCHREC / TEST。
- `docs/开发任务/V1-Core-TODO与预留清单.md:59,63-67`
  - `A15`、`A02`、`A10`、`A01` 的后续实现义务已登记到 `TODO-SEARCHREC-CONSUMER-001`、`TODO-AUD-EVENT-001`、`TODO-NOTIF-CONTRACT-001`、`TODO-TEST-CANONICAL-001`。
- `docs/开发准备/技术选型正式版.md:20`、`docs/开发任务/README.md:11`
  - README/推荐阅读层仍有坏链路，且会直接影响后续 Agent 的入场阅读顺序。

## 4. 问题清单

### [AA00-001]
- 严重级别：blocker
- 问题分类：canonical source 不唯一；说明文件未同步；承接不完整
- 关联阶段：全阶段 / process-governance
- 关联任务ID：TODO-PROC-BIL-001；Agent 执行流程（未入 CSV）
- 现象：
  流程文档、执行提示词、README、人工审批记录都要求后续 Agent 写入或先阅读 `docs/开发任务/V1-Core-实施进度日志.md`，但仓库内并不存在该文件，当前真实记录实际分散在 `V1-Core-实施进度日志-P1/P2/P3.md`。
- 正确口径：
  实施日志必须存在唯一、真实、可访问的 canonical source；要么恢复并统一到 `V1-Core-实施进度日志.md`，要么显式冻结“按批次分卷日志”的正式规则，并同步所有流程入口、README、TODO、审批依据和换手规则。
- 当前文档/任务现状：
  `docs/开发任务/Agent-开发与半人工审核流程.md:156-166,337-343` 强制写入和阅读 `开发任务/V1-Core-实施进度日志.md`；`docs/开发任务/AI-Agent-执行提示词.md:17-35,42,65` 继续把该文件当成必读、必写执行源；`docs/开发任务/README.md:11` 与 `docs/开发任务/V1-Core-人工审批记录.md:37` 把该文件当成索引或审批依据；`docs/开发任务/V1-Core-TODO与预留清单.md:43` 的 `TODO-PROC-BIL-001` 仍把该路径写为追溯来源；但 `docs/开发任务/V1-Core-实施进度日志-P2.md:705` 已明确写“路径不存在，按当前约定在 `V1-Core-实施进度日志-P2.md` 记录”，`docs/00-context/current-task-baseline-gap.md:33` 仍把缺失文件描述为现有载体。
- 已核对文件：
  `docs/开发任务/Agent-开发与半人工审核流程.md:156-166,337-343`；`docs/开发任务/AI-Agent-执行提示词.md:17-35,42,65`；`docs/开发任务/README.md:11`；`docs/开发任务/V1-Core-TODO与预留清单.md:43`；`docs/开发任务/V1-Core-人工审批记录.md:37`；`docs/开发任务/V1-Core-实施进度日志-P2.md:705`；`docs/00-context/current-task-baseline-gap.md:33`
- 风险：
  后续 Agent 会被引导去读写一个不存在的文件；批次计划、待审批记录、人工换手和偏移追溯没有唯一入口，CSV 虽然仍是执行源，但过程留痕源已失真，直接削弱后续阶段的执行可追责性。
- 建议修复方向：
  先裁决实施日志的正式载体。若继续用分卷日志，新增一个固定索引文件或恢复 `V1-Core-实施进度日志.md` 作为入口页，并统一更新流程文档、执行提示词、README、TODO、审批模板和 `current-task-baseline-gap.md`；同时给“实施日志权威源”补一个显式任务或 TODO，避免后续再次漂移。
- 是否必须在进入对应阶段前修复：yes

### [AA00-002]
- 严重级别：high
- 问题分类：technical_reference 不足；推荐阅读不足；说明文件未同步；承接不完整
- 关联阶段：CTX / NOTIF / SEARCHREC / cross-stage
- 关联任务ID：CTX-023；NOTIF-013；NOTIF-014；SEARCHREC-021
- 现象：
  唯一执行源中的多条 `technical_reference` 以及其上游设计文档仍引用当前仓库内不存在的文件或错误相对路径，导致后续 Agent 无法按 CSV 直接读取正确设计输入。
- 正确口径：
  CSV 中的 `technical_reference` 应该从 `docs/开发任务/` 基准目录可直接解析到真实文件；上游推荐阅读文档也必须指向当前仓库的真实脚本与 runbook，而不是历史目录或失效路径。
- 当前文档/任务现状：
  `docs/开发任务/v1-core-开发任务清单.csv:24` 的 `CTX-023` 仍引用 `../部署脚本/validate_database_migrations.sh`，该路径不存在；`docs/开发任务/v1-core-开发任务清单.csv:350` 的 `NOTIF-013` 把 `docs/02-openapi/README.md` 写成从 `docs/开发任务/` 出发的相对路径，解析结果为 `docs/开发任务/docs/02-openapi/README.md`，不存在；`docs/开发任务/v1-core-开发任务清单.csv:351` 的 `NOTIF-014` 同样把 `docs/05-test-cases/README.md` 和 `docs/04-runbooks/notification-worker.md` 写成失效路径；`docs/开发任务/v1-core-开发任务清单.csv:403` 的 `SEARCHREC-021` 把 `docs/04-runbooks/search-reindex.md` 写成失效路径；上游文档 `docs/开发准备/本地开发环境与中间件部署清单.md:138,319,323` 仍把推荐阅读指向 `../部署脚本/docker-compose.postgres-test.yml` 和 `../部署脚本/validate_database_migrations.sh`。
- 已核对文件：
  `docs/开发任务/v1-core-开发任务清单.csv:24,350-351,403`；`docs/开发准备/本地开发环境与中间件部署清单.md:138,319,323`
- 风险：
  后续 Agent 即使严格遵循 CSV，也会在最关键的设计参考环节撞到坏链接；这会直接削弱 `technical_reference` 的权威性，让任务清单不能作为可靠的执行入口。
- 建议修复方向：
  逐条修正 `CTX-023`、`NOTIF-013`、`NOTIF-014`、`SEARCHREC-021` 的 `technical_reference`，统一使用相对于 `docs/开发任务/` 的正确路径；同时同步修复 `本地开发环境与中间件部署清单.md` 中的历史脚本引用，并补一轮最小自动校验，至少能检查 CSV 技术引用是否落到真实文件。
- 是否必须在进入对应阶段前修复：yes

### [AA00-003]
- 严重级别：high
- 问题分类：命名漂移；canonical source 不唯一；technical_reference 不足；说明文件未同步
- 关联阶段：ENV / cross-stage
- 关联任务ID：ENV-058；TODO-ENV-NAMING-001
- 现象：
  配置冻结文档、runbook、compose、`.env.local`、校验脚本之间仍存在两套以上环境变量口径，且 `DATABASE_URL` 这类实际运行入口没有被清晰纳入配置权威源，后续开发容易按错名字接环境。
- 正确口径：
  需要明确一套唯一口径，或者明确写出“抽象配置名 -> 本地 compose 专用 env -> 运行时入口 env”的正式映射，并把数据库、MinIO、Keycloak、`DATABASE_URL` 一并收进同一权威源。
- 当前文档/任务现状：
  `docs/开发准备/配置项与密钥管理清单.md:180-184,202-203,230-231` 和 `docs/开发准备/本地开发环境与中间件部署清单.md:189-193,211-212,239-240` 仍使用 `PG_*`、`MINIO_ACCESS_KEY / MINIO_SECRET_KEY`、`KEYCLOAK_ADMIN_USERNAME / KEYCLOAK_ADMIN_PASSWORD`；但 `infra/docker/.env.local:49-55`、`infra/docker/docker-compose.local.yml:29-31,133-134,198-202`、`scripts/smoke-local.sh:46-48,65-66` 实际使用的是 `POSTGRES_*`、`MINIO_ROOT_*`、`KEYCLOAK_ADMIN`；`docs/04-runbooks/local-startup.md:19,37`、`scripts/seed-local-iam-test-identities.sh:6-8`、`scripts/check-query-compile.sh:9,15` 还把 `DATABASE_URL` 当成实际执行入口，但配置冻结文档没有把该入口收敛为正式映射；`docs/04-runbooks/secrets-policy.md:20,28-34` 进一步同时出现 `POSTGRES_PASSWORD` 与 `PG_PASSWORD`；而 `docs/开发任务/v1-core-开发任务清单.csv:115` 的 `ENV-058` 与 `docs/开发任务/V1-Core-TODO与预留清单.md:66` 的 `TODO-ENV-NAMING-001` 没有把这些具体漂移点列成必须同步的推荐阅读和验收对象。
- 已核对文件：
  `docs/开发准备/配置项与密钥管理清单.md:180-184,202-203,230-231`；`docs/开发准备/本地开发环境与中间件部署清单.md:189-193,211-212,239-240`；`infra/docker/.env.local:49-55`；`infra/docker/docker-compose.local.yml:29-31,133-134,198-202`；`scripts/smoke-local.sh:46-48,65-66`；`docs/04-runbooks/local-startup.md:19,37`；`scripts/seed-local-iam-test-identities.sh:6-8`；`scripts/check-query-compile.sh:9,15`；`docs/04-runbooks/secrets-policy.md:20,28-34`；`docs/开发任务/v1-core-开发任务清单.csv:115`；`docs/开发任务/V1-Core-TODO与预留清单.md:66`
- 风险：
  后续 Agent 很容易按冻结文档补 `PG_*`，而实际本地栈和 smoke/compose 读的是 `POSTGRES_*`；或者只看到 `KEYCLOAK_ADMIN_USERNAME`、`MINIO_ACCESS_KEY` 却无法让本地栈启动，最终造成环境初始化、runbook、脚本、README 的反复返工。
- 建议修复方向：
  由 `ENV-058` 明确裁决数据库、MinIO、Keycloak、`DATABASE_URL` 的统一命名与映射；把 `secrets-policy.md`、`.env.local`、`docker-compose.local.yml`、`local-startup.md`、`配置项与密钥管理清单.md`、`本地开发环境与中间件部署清单.md` 一并纳入 `technical_reference` 或推荐阅读；在 TODO 中补登记“必须补映射表与最小校验”的义务。
- 是否必须在进入对应阶段前修复：yes

### [AA00-004]
- 严重级别：medium
- 问题分类：阶段边界漂移；边界未冻结；说明文件未同步
- 关联阶段：CTX / ENV
- 关联任务ID：CTX-008
- 现象：
  冻结文档把运行模式收敛为 `local / staging / demo` 三套，但本地环境清单单独新增了 `staging-local` 小节，形成第四种看起来像正式模式的命名。
- 正确口径：
  如果 `staging-local` 只是本地 profile 组合或联调姿态，应作为 `local` 的子场景说明；如果它是正式运行模式，就必须回写 `CTX-008` 和 `run-modes.md`，不能只在环境文档里单点出现。
- 当前文档/任务现状：
  `docs/00-context/run-modes.md:5-21` 与 `docs/开发任务/v1-core-开发任务清单.csv:9` 都冻结为三套模式；`docs/00-context/local-deployment-boundary.md:5-10` 也把本地部署解释为通过 compose/profile 控制的本地变体；但 `docs/开发准备/本地开发环境与中间件部署清单.md:267-274` 另起 `## 9.2 staging-local`，并赋予其 Kafka/outbox/retry/dead-letter、搜索同步、Fabric 写入等单独特征。
- 已核对文件：
  `docs/00-context/run-modes.md:5-21`；`docs/开发任务/v1-core-开发任务清单.csv:9`；`docs/00-context/local-deployment-boundary.md:5-10`；`docs/开发准备/本地开发环境与中间件部署清单.md:267-274`
- 风险：
  后续 Agent 可能把 `staging-local` 当成第四套正式模式，引发配置、runbook、测试矩阵和环境变量边界的再漂移。
- 建议修复方向：
  把 `staging-local` 回收成 `local` 下的 profile 组合描述，或补一条显式冻结说明：“`staging-local` 不是正式运行模式，只是本地联调姿态”；若要保留术语，则必须同步更新 `CTX-008`、README、runbook 和相关任务引用。
- 是否必须在进入对应阶段前修复：no

### [AA00-005]
- 严重级别：medium
- 问题分类：test-case 承接缺失；说明文件未同步；命名漂移
- 关联阶段：TEST / CAT / SEARCHREC
- 关联任务ID：TEST-005；TEST-028
- 现象：
  当前测试样例仍指导宿主机进程连接 `127.0.0.1:9092`，但 runbook、端口矩阵和 compose 已冻结宿主机 Kafka 外部地址为 `127.0.0.1:9094`。
- 正确口径：
  宿主机启动示例、test-case、smoke、runbook、端口矩阵必须统一成“宿主机 `9094`，容器内 `kafka:9092`”。
- 当前文档/任务现状：
  `docs/05-test-cases/catalog-review-cases.md:23-31` 仍给出 `KAFKA_BROKERS=127.0.0.1:9092 KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:9092` 的示例；但 `docs/04-runbooks/local-startup.md:37`、`docs/04-runbooks/search-reindex.md:54`、`docs/04-runbooks/port-matrix.md:15` 以及 `infra/docker/docker-compose.local.yml:82-89` 已冻结宿主机 `9094`、容器内 `9092` 的双地址边界。
- 已核对文件：
  `docs/05-test-cases/catalog-review-cases.md:23-31`；`docs/04-runbooks/local-startup.md:37`；`docs/04-runbooks/search-reindex.md:54`；`docs/04-runbooks/port-matrix.md:15`；`infra/docker/docker-compose.local.yml:82-89`
- 风险：
  后续开发或手工验收如果按 `catalog-review-cases.md` 直接执行，很容易在本地把 Kafka 地址配错，导致把环境问题误判成实现问题。
- 建议修复方向：
  统一修正 `catalog-review-cases.md` 的宿主机 Kafka 示例；同时检查其他 test-cases/runbook 是否仍残留 `127.0.0.1:9092` 的宿主机写法，并把“宿主机 9094 / 容器内 9092”写成测试文档公共前置条件。
- 是否必须在进入对应阶段前修复：yes

### [AA00-006]
- 严重级别：low
- 问题分类：推荐阅读不足；说明文件未同步
- 关联阶段：CTX / onboarding
- 关联任务ID：CTX-008；README 索引层
- 现象：
  部分索引或推荐阅读文档仍保留坏链路，降低后续 Agent 的入场阅读效率。
- 正确口径：
  README/推荐阅读层至少要保证链接可达；即便不是 blocker，也不应保留明显失效的“建议先读”链接。
- 当前文档/任务现状：
  `docs/开发准备/技术选型正式版.md:20` 仍链接 `../开发准备/技术选型与技术路线总结.md`，该文件不存在；这不是唯一执行源问题，但它位于上游推荐阅读链路，后续 Agent 按索引阅读时会撞到空链接。
- 已核对文件：
  `docs/开发准备/技术选型正式版.md:20`
- 风险：
  会削弱索引文档的可信度，并增加后续阅读成本。
- 建议修复方向：
  删除该失效链接，或补成当前真实存在的技术路线总结文件；同步检查同一文档中的其他“推荐先读”链接是否仍有效。
- 是否必须在进入对应阶段前修复：no

### [AA00-007]
- 严重级别：suspect
- 问题分类：runbook 承接缺失；阶段边界漂移；suspect
- 关联阶段：ENV / local startup
- 关联任务ID：BOOT-009；ENV-033；ENV-052（suspect）
- 现象：
  `local-startup.md` 把 `./scripts/check-mock-payment.sh` 放进主线启动流程，但没有明确声明该检查依赖 `make up-demo` 或 `mocks/demo` profile；当前证据显示 mock payment provider 并不在默认 `core` profile 下。
- 正确口径：
  如果 Mock Payment 自检只在 `demo` 或 `mocks` profile 下可运行，runbook 应在步骤层面显式写出前置条件；如果希望它在默认 `up-local` 后就可用，则 compose profile 必须调整并同步更新文档。
- 当前文档/任务现状：
  `docs/04-runbooks/local-startup.md:3,46,56,63` 先把默认启动写成 `make up-local`，后面单列 `make up-demo`，再直接给出 `./scripts/check-mock-payment.sh`；`Makefile:7,19` 说明 `up-local` 默认只拉起 `core`，`up-demo` 才启用 `demo`；`infra/docker/docker-compose.local.yml:390-398` 把 `mock-payment-provider` 挂在 `profiles: [\"mocks\", \"demo\"]`；`scripts/check-mock-payment.sh:4` 默认直连 `http://127.0.0.1:8089`，未自行处理 profile 缺失场景。当前证据足以说明“存在前置条件未写清”的风险，但尚不能证明实际执行一定失败，因此列为 `suspect`。
- 已核对文件：
  `docs/04-runbooks/local-startup.md:3,46,56,63`；`Makefile:7,19`；`infra/docker/docker-compose.local.yml:390-398`；`scripts/check-mock-payment.sh:4`
- 风险：
  后续执行者可能在只启动 `up-local` 的情况下直接运行 mock payment 检查，并把 profile 缺失误判成服务异常。
- 建议修复方向：
  给 `local-startup.md` 的第 29 步补一句明确前置条件；如果确认默认 `up-local` 就应包含 mock provider，则应同步修改 compose profile 和对应任务说明。
- 是否必须在进入对应阶段前修复：no

## 5. 已收缩、无问题项

- `A01 / A02 / A10 / A15` 已收缩，无问题。
  证据：`docs/开发任务/v1-core-开发任务清单.csv:67-68,338-346,352,359-361,377,383,392,397,399,402,453` 已把这些问题修复文档纳入 `ENV / NOTIF / AUD / SEARCHREC / TEST` 的 `technical_reference`；`docs/开发任务/V1-Core-TODO与预留清单.md:59,63-67` 已同步登记 `TODO-SEARCHREC-CONSUMER-001`、`TODO-AUD-EVENT-001`、`TODO-NOTIF-CONTRACT-001`、`TODO-TEST-CANONICAL-001`。
- OpenAPI 的“当前权威源 vs 未来归档副本”边界已收缩，无问题。
  证据：`docs/02-openapi/README.md:5-24` 已明确“当前实现阶段以 `packages/openapi/*.yaml` 为准”；`docs/02-openapi/search.yaml:1-3` 明确声明自己不是当前设计参考；`docs/开发任务/V1-Core-TODO与预留清单.md:56,61,64` 已把 `AUD/Ops`、`SEARCHREC`、`NOTIF` 的未来 OpenAPI 补齐义务登记入 TODO。
- Test-cases README 对尚未落盘的通知/审计验收样例已收缩，无问题。
  证据：`docs/05-test-cases/README.md:19-20` 明确说明 `notification-cases.md` 与 `audit-consistency-cases.md` 尚未落盘且必须在对应实现批次补齐；`docs/开发任务/V1-Core-TODO与预留清单.md:57,64` 已把这两类补齐义务登记进 TODO。
- 当前已归档的 OpenAPI 副本与实现权威源并未出现批量漂移，无问题。
  证据：`docs/02-openapi/catalog.yaml:1-6` 与 `packages/openapi/catalog.yaml:1-6` 头部结构一致；`docs/02-openapi/README.md:19-24` 已把 `iam / billing / trade / delivery / catalog / recommendation` 标记为同步归档文件，仅 `search.yaml` 保留占位并显式声明非权威源。

## 6. 汇总结论

### 6.1 必须先修的问题

- `AA00-001`：实施日志 canonical source 缺失，流程入口、README、TODO、审批依据全部仍指向不存在的文件。
- `AA00-002`：CSV `technical_reference` 与上游推荐阅读存在失效路径，直接削弱 CSV 作为唯一执行源的可用性。
- `AA00-003`：环境变量与配置权威源不唯一，数据库/MinIO/Keycloak/`DATABASE_URL` 的命名映射未被冻结。
- `AA00-005`：test-case 仍指导宿主机连 Kafka `9092`，会在实际执行时误导本地联调。

### 6.2 可进入阶段但需尽快补齐的问题

- `AA00-004`：`staging-local` 与 `local / staging / demo` 三态冻结口径冲突，短期不一定阻塞，但会持续诱发阶段边界漂移。

### 6.3 仅文档命名 / 引用 / 说明收敛问题

- `AA00-006`：上游推荐阅读仍有坏链路，影响入场阅读效率，但短期不直接阻断实现。
- `AA00-007`：Mock Payment 启动步骤存在 profile 前置条件未写清的迹象，当前证据不足，先列为 `suspect`。

## 7. 对任务清单的修复建议

- 补 `technical_reference`：
  - 修正 `CTX-023`、`NOTIF-013`、`NOTIF-014`、`SEARCHREC-021` 的路径，使之都能从 `docs/开发任务/` 正确解析到真实文件。
  - 为 `ENV-058` 增补 `docs/04-runbooks/secrets-policy.md`、`infra/docker/.env.local`、`infra/docker/docker-compose.local.yml`、`docs/04-runbooks/local-startup.md` 作为推荐阅读。
- 补 `definition_of_done / acceptance_check`：
  - 给 `ENV-058` 增加“必须给出抽象 env 名 -> 本地 compose env -> 运行时入口 env 的映射表，并由脚本或文档校验”的 DoD。
  - 给涉及 test-case 入口的 `TEST-005 / TEST-028` 增加“宿主机端口、容器内地址、profile 前置条件必须与 runbook 一致”的验收要求。
- 建议新增 task：
  - 新增一个 `CTX / PROC` 级任务，专门收敛“实施日志 canonical source / 批次日志入口 / README 与流程同步”问题，避免继续把这个缺口挂在流程文档外面。
  - 可新增一个轻量文档校验任务，自动检查 CSV `technical_reference` 与关键 README 链接是否可达。
- 建议补 TODO：
  - 新增“实施日志权威源收敛”类 TODO，明确在流程治理层补 canonical source，而不是继续依赖隐含约定。
  - 若 `staging-local` 继续保留，新增 TODO 或规则说明，明确其不是第四套正式运行模式。
- 需要同步更新的 runbook / OpenAPI README / test-case README：
  - `docs/04-runbooks/local-startup.md`
  - `docs/04-runbooks/search-reindex.md`
  - `docs/04-runbooks/secrets-policy.md`
  - `docs/04-runbooks/port-matrix.md`
  - `docs/05-test-cases/catalog-review-cases.md`
  - `docs/开发准备/配置项与密钥管理清单.md`
  - `docs/开发准备/本地开发环境与中间件部署清单.md`
  - `docs/开发任务/README.md`
  - `docs/开发任务/Agent-开发与半人工审核流程.md`
  - `docs/开发任务/AI-Agent-执行提示词.md`

## 8. 推荐后续处理顺序

1. 先修 `AA00-001` 与 `AA00-002`。
   先把实施日志权威源与 CSV 技术引用修正，否则后续 Agent 连“该看什么、该写到哪里”都没有稳定答案。
2. 再修 `AA00-003`。
   环境命名和 `DATABASE_URL`/`PG_*`/`POSTGRES_*`/`MINIO_*`/`KEYCLOAK_*` 映射是后续所有 runbook、脚本、local smoke 的公共底座。
3. 随后修 `AA00-005` 与 `AA00-004`。
   先把 test-case 与端口矩阵拉齐，再把 `staging-local` 收回三态冻结边界，防止即将进入的联调/验收阶段继续读错模式与环境。
4. 最后处理 `AA00-006` 与 `AA00-007`。
   这两项短期不阻塞主线，但应该在下一轮文档收口时一起清掉，避免继续损伤索引层可信度。
