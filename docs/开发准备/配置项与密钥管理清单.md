# 配置项与密钥管理清单

## 1. 文档定位

本文件用于冻结当前阶段平台配置项分类、密钥分类、存放位置、注入方式、轮换要求和禁止事项。

本文件服务于以下工作：

- 本地开发配置管理
- 测试环境配置管理
- 后续生产环境密钥治理
- 证书、令牌、下载密钥和外部集成密钥管理

本文件不替代 KMS/HSM 实现方案，不替代具体 Vault 或 Secret Manager 选型文档。

阅读入口约束：

- 当前阶段的配置与密钥治理以 `../全集成文档/数据交易平台-全集成基线-V1.md` 为主阅读入口。
- 本文件只冻结 `V1` 的配置分层、敏感信息边界和本地/测试环境管理要求。

上位文档：

- [数据交易平台-全集成基线-V1.md](../全集成文档/数据交易平台-全集成基线-V1.md)
- [本地开发环境与中间件部署清单.md](../开发准备/本地开发环境与中间件部署清单.md)
- [技术选型正式版.md](../开发准备/技术选型正式版.md)
- [服务清单与服务边界正式版.md](../开发准备/服务清单与服务边界正式版.md)

## 2. 总原则

### 2.1 当前冻结原则

- 配置与密钥必须分离管理
- 数据对象与长期解密能力不得由同一平面永久独占
- 证书、令牌、下载授权、服务签名密钥必须支持轮换、吊销、过期和异常停用
- 高风险配置修改必须进入审计并支持 step-up

### 2.2 当前阶段现实约束

当前阶段允许：

- 采用可审计的软件密钥管理方式
- 在本地和测试环境使用 `.env` 或容器 secret 文件

当前阶段不允许：

- 将真实生产密钥硬编码进代码仓
- 将生产证书私钥直接放在前端或普通容器镜像中
- 将数据库密码、支付签名密钥、Fabric 私钥直接打印到日志

## 3. 配置分类

统一把配置分成 4 类：

1. 普通运行配置
2. 敏感配置
3. 密钥与证书材料
4. 可派生临时凭证

## 4. 普通运行配置

## 4.1 定义

不直接导致越权、签名、解密或支付风险的配置。

示例：

- `APP_ENV`
- `APP_PORT`
- `LOG_LEVEL`
- `BASE_URL`
- `POSTGRES_PORT`
- `DATABASE_URL`
- `KAFKA_BROKERS`
- `MINIO_ENDPOINT`
- `OPENSEARCH_ENDPOINT`
- `KEYCLOAK_BASE_URL`

## 4.2 存放方式

允许使用：

- `.env.local`
- Docker Compose 环境变量
- Kubernetes ConfigMap

## 5. 敏感配置

## 5.1 定义

虽不属于私钥或证书材料，但泄漏后可导致环境被接管或外部系统被滥用的配置。

示例：

- `POSTGRES_PASSWORD`
- `DATABASE_URL`（若包含密码，按敏感配置处理）
- `REDIS_PASSWORD`
- `OPENSEARCH_PASSWORD`
- `KEYCLOAK_CLIENT_SECRET`
- `KEYCLOAK_ADMIN_PASSWORD`
- `MOCK_PAYMENT_SIGNING_SECRET`
- 外部支付通道 `api_key / client_secret`

## 5.2 存放方式

当前建议：

- 本地：`.env.local.secret` 或容器 secret 文件
- 测试：Docker secret / 密码文件 / 私有 CI secret
- 生产：Secret Manager / Vault / KMS 集成

禁止：

- 提交到 Git
- 放进前端构建产物
- 写入截图、工单正文、普通日志

## 6. 密钥与证书材料

## 6.1 定义

可直接用于签名、解密、证书认证或生成下游高权限访问能力的材料。

示例：

- `FABRIC_PRIVATE_KEY_PATH`
- `FABRIC_SIGN_CERT_PATH`
- `FABRIC_TLS_CERT_PATH`
- 下载密钥封装材料
- 服务签名私钥
- 渠道签名证书或私钥
- 未来 KMS 主密钥引用

## 6.2 当前处理原则

- 只能以文件或 secret volume 形式注入
- 不允许以内联字符串硬编码到配置文件
- 不允许经前端传递
- 不允许直接进入普通数据库字段明文

## 6.3 数据库存放边界

数据库允许存：

- `certificate_digest`
- `secret_ref`
- `client_secret_ref`
- 密钥材料引用标识
- 加密后的 key envelope

数据库不允许存：

- 明文私钥
- 明文渠道签名密钥
- 明文长期下载解密密钥

## 7. 临时凭证

## 7.1 定义

生命周期短、可吊销、用于特定高风险或交付动作的临时凭证。

示例：

- `X-Step-Up-Token`
- 下载票据
- 共享开通临时 token
- 模板查询授权 token
- 沙箱会话 token

## 7.2 管理原则

- 允许落库为摘要或受控记录
- 必须有失效时间
- 必须支持主动吊销
- 必须留审计

## 8. 当前主要配置项清单

## 8.0 命名与映射正式规则

数据库、MinIO、Keycloak 的配置必须区分以下 3 层，不允许再把不同角色的变量名混成一套默认口径：

1. 本地基础设施 bootstrap 变量
2. 应用 / 脚本运行时入口变量
3. CLI / 临时调试兼容变量

正式冻结规则如下：

### 8.0.1 数据库

- 本地 compose / 容器 bootstrap：`POSTGRES_DB`、`POSTGRES_USER`、`POSTGRES_PASSWORD`、`POSTGRES_PORT`
- 应用与大部分脚本正式入口：`DATABASE_URL`
- `psql / libpq` 临时调试兼容：`PGHOST`、`PGPORT`、`PGDATABASE`、`PGUSER`、`PGPASSWORD`
- 冻结约束：
  - `DATABASE_URL` 是应用和大部分脚本的正式数据库入口
  - `POSTGRES_*` 只用于本地 PostgreSQL / Keycloak bootstrap，不替代运行时入口
  - `PG_HOST / PG_PORT / PG_DATABASE / PG_USER / PG_PASSWORD` 不再作为正式主配置名

### 8.0.2 MinIO

- 本地 compose / 容器 bootstrap：`MINIO_ROOT_USER`、`MINIO_ROOT_PASSWORD`
- 应用与脚本运行时入口：`MINIO_ENDPOINT`、`MINIO_ACCESS_KEY`、`MINIO_SECRET_KEY`
- 本地默认映射：
  - `MINIO_ACCESS_KEY = MINIO_ROOT_USER`
  - `MINIO_SECRET_KEY = MINIO_ROOT_PASSWORD`
- 冻结约束：
  - `MINIO_ROOT_*` 只用于 MinIO 服务端 bootstrap
  - 运行时客户端和脚本应优先读取 `MINIO_ACCESS_KEY / MINIO_SECRET_KEY`

### 8.0.3 Keycloak

- 本地 compose / 容器 bootstrap：`KEYCLOAK_ADMIN`、`KEYCLOAK_ADMIN_PASSWORD`
- 应用与脚本运行时入口：`KEYCLOAK_BASE_URL`、`KEYCLOAK_REALM`、`KEYCLOAK_CLIENT_ID`、`KEYCLOAK_CLIENT_SECRET`
- 冻结约束：
  - `KEYCLOAK_ADMIN` 只用于本地 Keycloak bootstrap
  - `KEYCLOAK_ADMIN_USERNAME` 不再作为正式主配置名
  - realm 运行时口径统一使用 `KEYCLOAK_REALM`

## 8.1 数据库

- `POSTGRES_DB`
- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `POSTGRES_PORT`
- `DATABASE_URL`
- `PGHOST`
- `PGPORT`
- `PGDATABASE`
- `PGUSER`
- `PGPASSWORD`

## 8.2 Redis

- `REDIS_HOST`
- `REDIS_PORT`
- `REDIS_PASSWORD`

## 8.3 Kafka

- `KAFKA_BROKERS`
- `KAFKA_CLIENT_ID`
- `KAFKA_CONSUMER_GROUP`

## 8.4 MinIO

- `MINIO_ROOT_USER`
- `MINIO_ROOT_PASSWORD`
- `MINIO_ENDPOINT`
- `MINIO_ACCESS_KEY`
- `MINIO_SECRET_KEY`
- `BUCKET_RAW_DATA`
- `BUCKET_PREVIEW_ARTIFACTS`
- `BUCKET_DELIVERY_OBJECTS`
- `BUCKET_REPORT_RESULTS`
- `BUCKET_EVIDENCE_PACKAGES`
- `BUCKET_MODEL_ARTIFACTS`

## 8.5 OpenSearch

- `OPENSEARCH_ENDPOINT`
- `OPENSEARCH_USERNAME`
- `OPENSEARCH_PASSWORD`
- `INDEX_ALIAS_PRODUCT_SEARCH_READ`
- `INDEX_ALIAS_PRODUCT_SEARCH_WRITE`
- `INDEX_ALIAS_SELLER_SEARCH_READ`
- `INDEX_ALIAS_SELLER_SEARCH_WRITE`
- `INDEX_NAME_PRODUCT_SEARCH_BOOTSTRAP`
- `INDEX_NAME_SELLER_SEARCH_BOOTSTRAP`
- `INDEX_NAME_SEARCH_SYNC_JOBS`

## 8.6 Keycloak

- `KEYCLOAK_BASE_URL`
- `KEYCLOAK_REALM`
- `KEYCLOAK_CLIENT_ID`
- `KEYCLOAK_CLIENT_SECRET`
- `KEYCLOAK_ADMIN`
- `KEYCLOAK_ADMIN_PASSWORD`

## 8.7 Mock Payment Provider

- `MOCK_PAYMENT_BASE_URL`
- `MOCK_PAYMENT_SIGNING_SECRET`

## 8.8 Fabric

- `FABRIC_GATEWAY_ENDPOINT`
- `FABRIC_CHANNEL_NAME`
- `FABRIC_CHAINCODE_NAME`
- `FABRIC_MSP_ID`
- `FABRIC_TLS_CERT_PATH`
- `FABRIC_SIGN_CERT_PATH`
- `FABRIC_PRIVATE_KEY_PATH`
- `FABRIC_CA_ADMIN_PORT`
- `FABRIC_CA_ADMIN_BASE_URL`
- `FABRIC_CA_ADMIN_MODE`

## 9. 环境文件建议

建议分层：

- `.env.example`
  - 只放键名与非敏感默认值
- `infra/docker/.env.local`
  - 本地 bootstrap 变量与运行时入口样例的正式落点
- `.env.local.secret`
  - 本地敏感配置
- `secrets/`
  - 本地证书和私钥文件目录，不进 Git

## 10. 注入方式建议

## 10.1 本地开发

允许：

- `.env.local`
- `.env.local.secret`
- 挂载 `secrets/` 目录

## 10.2 Docker Compose

建议：

- 普通配置走 `environment`
- 敏感配置优先走 `env_file` 或 secret file
- 证书和私钥走 volume mount

## 10.3 测试与生产

建议：

- 普通配置：ConfigMap / 环境变量
- 敏感配置：Secret Manager / Vault / Secret volume
- 密钥材料：独立 secret mount 或专用密钥服务

## 11. 轮换与吊销规则

## 11.1 最低要求

- 核心密钥至少每 `90` 天轮换一次
- 证书必须支持吊销与失效
- 下载授权、step-up token、共享 token 必须具备短时效

## 11.2 触发轮换的情况

- 人员变更
- 环境泄漏怀疑
- 签名失败异常
- 证书异常吊销
- 审计命中高风险事件

## 12. 日志与审计要求

必须审计：

- 密钥轮换
- 证书签发/吊销
- 高风险配置修改
- 支付通道密钥变更
- 下载密钥封装策略修改

日志中必须脱敏：

- 数据库密码
- 支付密钥
- 客户端密钥
- 证书私钥路径后的文件内容
- 下载票据完整值

## 13. 当前禁止事项

- 把真实生产密钥写入 `.env.example`
- 把 `client_secret`、支付签名密钥、Fabric 私钥提交进 Git
- 在代码里直接写死 `POSTGRES_PASSWORD`、带密码的 `DATABASE_URL`、`KEYCLOAK_CLIENT_SECRET`、`MOCK_PAYMENT_SIGNING_SECRET`
- 在接口返回中暴露密钥原文
- 在审计导出包中默认带出私钥材料

## 14. 后续演进建议

当前阶段先采用可审计的软件密钥管理；后续建议逐步演进到：

- KMS/HSM
- 统一 Secret Manager
- 更细粒度的证书签发审批
- 自动轮换流水线

## 15. 一句话结论

当前阶段应统一理解为：

**先区分 bootstrap 变量、运行时入口变量和 CLI 兼容变量，再统一映射；`DATABASE_URL`、`MINIO_ACCESS_KEY / SECRET_KEY`、`KEYCLOAK_REALM` 是正式运行时入口，敏感配置进 secret，私钥与证书材料只走文件或专用密钥管理路径；所有高风险密钥动作必须可审计、可轮换、可吊销。**
