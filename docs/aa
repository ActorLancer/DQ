 这轮我也复核过，之前你盯的这些混用词在这四章里已经基本收干净。
  如果继续，下一步就不该再做术语清洗了，而应该做一件更实用的事：从这份清理版里再抽一
  份“研发冻结版”，只保留研发真正要用的章节和真值表，避免团队继续从 5000 多行文档里自己
  找主轴。

• 下一步不该继续在主 PRD 上做小修了，应该转到“冻结传播”和“执行落地”。

  优先顺序我建议这样排：

  1. 同步接口协议

  - 把刚冻结的规则同步到接口层：
      - Product vs SKU 事实源边界
      - pricing_mode vs Billing Event
      - API 类 SKU 默认权利与模板命名
  - 否则接口文档会继续和主 PRD 漂移。

  2. 出一份真正给研发用的冻结表版

  - 现在的“研发唯一真值摘录版”已经能用了，但更像“导航 + 摘要”。
  - 下一份应该是更短的执行版，只放：
      - 正式枚举
      - 标准 SKU
      - 模板 ID
      - 状态字段
      - Product/SKU 事实源
      - 不允许混用的旧字段
  - 这样研发、测试、法务拿到就能直接用。

  3. 做一次跨文档一致性总复核

  - 范围建议只扫这些目录：
      - 正式PRD
      - 领域模型
      - 权限设计
      - 数据库设计
      - 接口协议
  - 目标不是再审业务，而是专抓冻结口径有没有残留旧词。

  4. 再决定是否进入技术方案或开发

  - 如果你准备进入设计/开发阶段，下一步就应该开始：
      - 接口清单冻结
      - 数据库迁移修正
      - 服务边界拆分
      - 页面与状态流实现拆解

  如果只选一个最该做的，我建议先做第 1 步：同步接口协议。
  因为接口层最容易把已经冻结的模型重新搞乱。




• 我已经继续推进到“开发准备”阶段，并新增了一份正式实施清单：

  - 开发准备/V1-开发实施清单.md

  这份文件做的事很明确：

  - 把当前冻结包里的单一事实源列成开发前必须只读的真值
  - 按 V1-Core 顺序拆成：
      - 主数据与目录骨架
      - 交易闭环骨架
      - 交付与控制面
      - 审计、一致性与支付
  - 给出接口、数据库、页面、联调前的检查清单
  - 明确哪些内容现在不应抢跑实现

  另外，我顺手又做了一次全套冻结口径总复核。结果是：

  - 当前这套“正式清理版 + 正式PRD + 业务流程 + 页面说明 + 领域模型 + 权限设计 + 接口协议”里的高风险漂移，已经基本收完。
  - 真正还保留旧名的地方，只剩领域模型里有意保留的“实现层表名说明”，这不再是冻结口径冲突。

  如果你继续，我建议下一步直接进入其中一个具体执行面，不要再泛化：

  1. 冻结数据库表与迁移任务拆分
  2. 冻结接口清单与请求响应 schema
  3. 冻结页面开发任务与状态流
  4. 冻结联调与测试用例矩阵

  如果你不指定，我建议先做第 2 个：接口清单与 schema 冻结。

---

 还缺，而且缺的不是“业务文档”，而是把文档变成可开发输入的那几类文件。

  你现在这套文档已经很完整，PRD / 正式PRD / 业务流程 / 页面说明 / 领域模型 / 权限设计 / 数据库设计 / 接口协议 / 技术选
  型 都有了。
  如果按“能否真正开始研发”来判断，主要还缺下面这些正式文件：

  P0，建议先补

  - 服务清单与服务边界正式版
      - 现在有技术选型，但还没有把每个服务的职责、输入输出、归属语言、是否主状态源写成正式边界文件。
  - 接口清单与 OpenAPI/Schema 冻结表
      - 你有接口协议，但还没有真正可给前后端联调的请求/响应 schema、分页、过滤、错误码、示例。
  - 事件模型与 Topic 清单正式版
      - 现在有一致性和 Kafka 口径，但还缺：
          - topic 列表
          - producer/consumer 归属
          - key 规则
          - payload schema version
          - 幂等键
          - DLQ 路由
  - 统一错误码字典正式版
      - 主 PRD 有错误码原则，但还没有平台级错误码总表。
  - 测试用例矩阵正式版
      - 现在有测试要求，但还缺真正按模块/场景/SKU/异常拆开的测试用例基线。

  P1，进入开发后很快就会需要

  - 本地开发环境与中间件部署清单
      - 含 Docker Compose、依赖顺序、环境变量说明、哪些服务本地可 mock。
  - 配置项与密钥管理清单
      - 哪些配置是必填、哪些按环境切换、哪些必须进密钥管理。
  - 仓库拆分与目录结构建议
      - 单仓/多仓、服务目录、共享协议目录、数据库迁移目录、前端应用目录。
  - 联调说明书
      - 前后端、支付 mock、Fabric 测试网、搜索、推荐、审计联查怎么联调。
  - 状态机实现映射表
      - 把 PRD 的状态机映射到代码对象、数据库字段、事件名、自动动作。

  P2，首轮开发不是阻塞，但越早越好

  - ADR（架构决策记录）
      - 至少记录：
          - 为什么主后端选 Rust
          - 为什么 Fabric 用 Go
          - 为什么搜索用 OpenSearch
          - 为什么 V1 不上 OPA/TEE/FL/MPC
  - 运维 Runbook / 故障处理手册
      - Kafka 堆积、索引延迟、链确认失败、支付回调乱序怎么处理。
  - 数据迁移与版本发布操作手册
      - 虽然你有 SQL 升降级脚本，但还缺操作层面的发布步骤。

  我认为当前最缺的 5 份文件
  如果只按优先级排最值得立刻补的，就是这 5 个：

  1. 服务清单与服务边界正式版
  2. 接口清单与 OpenAPI/Schema 冻结表
  3. 事件模型与 Topic 清单正式版
  4. 统一错误码字典正式版
  5. 测试用例矩阵正式版

  一句话判断

  - 你现在不缺产品设计文件
  - 你现在缺的是研发冻结文件和联调落地文件

  如果你愿意，我建议下一步直接开始生成这 5 份。
  最合理的顺序是：

  1. 服务清单与服务边界正式版
  2. 接口清单与 OpenAPI/Schema 冻结表
  3. 事件模型与 Topic 清单正式版
  4. 统一错误码字典正式版
  5. 测试用例矩阵正式版

---

缺，而且缺的是“研发冻结文件”，不是继续补 PRD。

  按你现在这套文档包，真正还缺的开发输入主要是这 5 份，优先级最高：

  1. 服务清单与服务边界正式版

  - 每个服务做什么、不做什么
  - 谁是状态事实源
  - 用什么语言实现
  - 服务之间怎么调用

  2. 接口清单与 OpenAPI/Schema 冻结表

  - 请求/响应 schema
  - 分页、过滤、排序
  - 枚举、错误码、示例
  - 哪些接口是同步、哪些是异步

  3. 事件模型与 Topic 清单正式版

  - Kafka topic 列表
  - producer / consumer 归属
  - key 规则
  - payload schema version
  - 幂等键、重试、DLQ 规则

  4. 统一错误码字典正式版

  - 平台级错误码总表
  - 模块归属
  - 前后端统一含义
  - 排查建议

  5. 测试用例矩阵正式版

  - 按模块、SKU、状态机、异常场景拆开
  - 覆盖支付回调、链上链下不一致、撤权失败、搜索延迟、争议冻结等

  第二梯队建议补：

  - 本地开发环境与中间件部署清单
  - 配置项与密钥管理清单
  - 仓库拆分与目录结构建议
  - 联调说明书
  - 状态机实现映射表
  - ADR 架构决策记录

  一句话判断：

  - 你现在不缺产品和规则文档
  - 你现在缺的是把文档变成可开发、可联调、可测试的工程冻结文件

  如果继续，我建议直接从第 1 份开始生成：
  开发准备/服务清单与服务边界正式版.md
