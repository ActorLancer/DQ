# 任务基线差异清单（CTX-024）

## 1. 对照范围

- `docs/开发任务/`
- `docs/开发准备/`
- `docs/开发前设计文档/`
- `开发任务/v1-core-开发任务清单.csv`（执行源）

输出分类：

- 已存在可复用
- 需迁移
- 需重写
- 可删除重复

## 2. 已存在可复用

1. `docs/开发任务/` 主流程文件齐全（CSV、阅读版、流程、提示词、日志、TODO、审批）。
2. `docs/开发准备/` 10 份冻结准备文档齐全，可直接作为工程约束输入。
3. `docs/开发前设计文档/README.md` 及 5 份入口文件已形成“阅读导航层”，适合作为只读入口。
4. `docs/00-context/` 已完成 `CTX-001~CTX-012` 冻结文档，可继续作为上下文主入口。

## 3. 需迁移

1. 本地部署核心资产当前在 `部署脚本/`，而后续任务多处交付目标指向 `infra/docker/`：
   - 建议后续 ENV 批次按任务要求逐步迁移并保持兼容软链接或双入口过渡。
2. 观测配置已在 `infra/docker/monitoring/`，但 compose 主文件在 `部署脚本/`：
   - 建议统一到 `infra/docker/` 后由根入口调用。

## 4. 需重写

1. `docs/开发任务/V1-Core-实施进度日志.md` 已冻结为唯一入口页，`P1/P2/P3` 为分卷正文：
   - 后续若新增分卷，必须先更新入口页中的“当前活动分卷”和分卷列表，避免再次出现不存在路径或写死分卷的漂移。
2. 多个批次“计划中”记录中的审批字段被提前写为“通过”：
   - 建议后续保持“计划中=待审批”，仅在“待审批”记录中更新最终结论。

## 5. 可删除重复（暂不执行删除）

1. `docs/开发前设计文档/` 与 `docs/开发准备/` 在阅读入口层面存在内容重复描述：
   - 可保留 `开发前设计文档` 作为导航，不再扩展实体规则正文。
2. 若后续形成统一 runbook，可把 `部署脚本/LOCAL_DEV.md` 的重复说明并入正式 runbook，再决定是否删除旧说明。

## 6. 执行建议（面向后续批次）

1. 继续坚持“CSV 为唯一执行源 + 冻结文档裁决”的口径，不新增平行任务源。
2. 针对 `CTX-014`，直接引用本文件与 `current-repo-assets.md`、`current-local-stack-assets.md`，避免重复盘点。
3. 对路径收敛类任务优先采用“迁移 + 兼容”策略，避免一次性重排目录造成联调中断。
